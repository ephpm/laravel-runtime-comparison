name: laravel-benchmark-ephpm

services:
  app:
    build:
      context: ../..
      dockerfile: runtimes/ephpm/Dockerfile
    ports:
      - "8086:8000"
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      LOG_CHANNEL: stderr
