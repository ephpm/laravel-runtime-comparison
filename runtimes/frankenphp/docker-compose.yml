name: laravel-benchmark-frankenphp

services:
  app:
    build:
      context: ../..
      dockerfile: runtimes/frankenphp/Dockerfile
    ports:
      - "8081:8000"
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      LOG_CHANNEL: stderr
      OCTANE_SERVER: frankenphp
