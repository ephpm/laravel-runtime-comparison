name: laravel-benchmark-openswoole

services:
  app:
    build:
      context: ../..
      dockerfile: runtimes/openswoole/Dockerfile
    ports:
      - "8083:8000"
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      LOG_CHANNEL: stderr
      OCTANE_SERVER: swoole
