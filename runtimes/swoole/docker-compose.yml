name: laravel-benchmark-swoole

services:
  app:
    build:
      context: ../..
      dockerfile: runtimes/swoole/Dockerfile
    ports:
      - "8082:8000"
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      LOG_CHANNEL: stderr
      OCTANE_SERVER: swoole
